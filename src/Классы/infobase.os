Перем Параметры;
Перем Исполнитель;

Процедура ПриСозданииОбъекта(Знач ВхПараметры, ВхИсполнитель)

	Параметры = Новый Соответствие;
	Параметры.Вставить("cluster", ВхПараметры.Получить("cluster"));
	Параметры.Вставить("cluster-user", ВхПараметры.Получить("cluster-user"));
	Параметры.Вставить("cluster-pwd", ВхПараметры.Получить("cluster-pwd"));

	Исполнитель = ВхИсполнитель;

КонецПроцедуры	

Функция summary() Экспорт

	Возврат Новый summary(Параметры, Исполнитель);

КонецФункции	